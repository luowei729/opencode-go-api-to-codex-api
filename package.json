{
  "name": "opencode-go-to-codex-api",
  "version": "1.1.0",
  "description": "API proxy that converts OpenCode Go API to Codex (OpenAI-compatible) API format. Cloudflare Workers deployment.",
  "main": "worker/index.js",
  "scripts": {
    "test": "node --test",
    "cf:dev": "npx wrangler dev",
    "cf:deploy": "npx wrangler deploy"
  },
  "keywords": [
    "opencode",
    "codex",
    "api",
    "proxy",
    "cloudflare-workers"
  ],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
